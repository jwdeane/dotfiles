# create deno bin directory
mkdir -p ~/.deno/bin

# create golang bin directory
mkdir -p ~/.go/bin

# create volta directory
mkdir -p ~/.config/volta/bin

# create zsh config directories
mkdir -p ~/.config/zsh/{completions,functions}
